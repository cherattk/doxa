$('#logdiv').on('submit',function(e) {
    e.preventDefault();
    alert('Connection Utilisateur');
});
/*
var boardmenu = document.getElementById('boardmenu');
        boardmenu.style.visibility = "hidden";

        document.getElementById('logboard').onclick = function(e){
            e.stopPropagation();
            if( boardmenu.style.visibility === 'visible'){
                boardmenu.style.visibility = 'hidden';
            }else{
                boardmenu.style.visibility = 'visible';
            }
        };
        var appmenu = document.getElementById('appmenu');
                    appmenu.style.visibility = "hidden";
                    
                document.getElementById('appmenu-icon').onclick = function(e){
                    e.stopPropagation();
                    if( appmenu.style.visibility === 'visible'){
                        appmenu.style.visibility = 'hidden';
                    }else{
                        appmenu.style.visibility = 'visible';
                    }
                };
        document.onclick = function(e){
            var menu = null;
            if(e.tagrget){
                menu  = value;
            }
            if( menu.style.visibility === 'visible'){
                menu.style.visibility = 'hidden';
            };
        };

*/